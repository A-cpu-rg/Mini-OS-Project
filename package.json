{
  "name": "mini-os-project",
  "version": "2.0.0",
  "description": "MiniOS — A C-built mini operating system with React dashboard",
  "private": true,
  "scripts": {
    "build:c":   "make -C . clean && make -C .",
    "server":    "node server/server.js",
    "ui":        "npm run dev --prefix ui",
    "dev":       "concurrently --names \"C-BUILD,SERVER,UI\" --prefix-colors \"cyan,green,magenta\" \"npm run build:c\" \"npm run server\" \"npm run ui\"",
    "start":     "concurrently --names \"SERVER,UI\" --prefix-colors \"green,magenta\" \"npm run server\" \"npm run ui\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
